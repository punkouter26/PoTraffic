// Strongly-typed entity identifiers are part of the shared vocabulary every DTO in this
// assembly speaks (Rule 1 — Domain Integrity), so they are imported once here rather
// than repeated at the top of each DTO file.
global using PoTraffic.Shared.Ids;
