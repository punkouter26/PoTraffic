<Project Sdk="Microsoft.NET.Sdk">

  <!--
    PoTraffic.IntegrationTests — Integration tier (50% of the test pyramid).

    Spins up a real WebApplicationFactory<Program> in the Testing environment
    and a Testcontainers-managed Azurite instance for Table Storage.

    Tier ratio (100/50/25/25) enforced by the solution layout:
      • PoTraffic.UnitTests        — Unit tier (no I/O, FluentValidation, DTO mapping).
      • PoTraffic.IntegrationTests            — Integration tier (this file, WAF + Azurite).
      • PoTraffic.E2ETests/Api    — E2E API tier (HTTP against a live API).
      • PoTraffic.E2ETests/Ui     — E2E UI tier (Playwright in real browsers).
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>PoTraffic.IntegrationTests</RootNamespace>
    <AssemblyName>PoTraffic.IntegrationTests</AssemblyName>
    <IsPackable>false</IsPackable>
    <NoWarn>$(NoWarn);xUnit1031</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\PoTraffic.Shared\PoTraffic.Shared.csproj" />
    <ProjectReference Include="..\..\src\PoTraffic.API\PoTraffic.API.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="FluentAssertions" />
    <PackageReference Include="Testcontainers" />
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
    <PackageReference Include="FluentValidation" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="xunit.runner.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>