{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "PostToolUse hook: run slopwatch after every Edit/Write/MultiEdit to block new LLM-introduced slop (Rule 12 / slopwatch skill). The --hook flag scopes analysis to git dirty files only and exits non-zero on errors so Claude sees and fixes them immediately.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "dotnet slopwatch analyze -d . --hook --fail-on warning",
            "timeout": 60000
          }
        ]
      }
    ]
  }
}
